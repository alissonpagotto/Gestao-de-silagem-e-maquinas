import { supabase, isSupabaseConfigured, SUPABASE_URL, SUPABASE_ANON_KEY, testSupabaseConnection } from './supabase';

export { 
  supabase, 
  isSupabaseConfigured, 
  SUPABASE_URL, 
  SUPABASE_ANON_KEY, 
  testSupabaseConnection 
};

export default supabase;
